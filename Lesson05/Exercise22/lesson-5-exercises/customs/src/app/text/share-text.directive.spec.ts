import { ShareTextDirective } from './share-text.directive';

describe('ShareTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ShareTextDirective();
    expect(directive).toBeTruthy();
  });
});
